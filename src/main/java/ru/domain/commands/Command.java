package ru.domain.commands;

/**
 * Создаем интерфейс для паттерна проектирования 'Command Pattern'.
 * Будем определять классы команд для каждый операции.
 */
public interface Command {
    void execute();
}
